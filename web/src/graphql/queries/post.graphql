query Readpost($id: Int!) {
    Readpost(identifier: $id) {
        id
        title
        text
        textSnippet
        voteStatus
        createdAt
        points
        creator{
            id
            username
        }
    }
}